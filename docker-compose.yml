version: "3.9"
services:
  staking-api-service:
    image: babylonchain/staking-api-service:latest
    container_name: staking-api-service
    ports:
      - "80:8090"
    environment:
      - CONFIG=/home/api/config.yml
    volumes:
      - ./config/api/config-docker.yml:/home/api/config.yml:Z
  mongodb:
    image: mongo:latest
    container_name: mongodb
    hostname: mongodb
    ports:
      - "27017:27017"
    volumes:
      - ./bin/init-mongo.sh:/init-mongo.sh
    entrypoint: [ "/init-mongo.sh" ]
